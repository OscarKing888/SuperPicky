# SuperPicky 依赖包
# Python 版本要求: 3.12+
#
# 使用虚拟环境:
#   创建: python3.12 -m venv .venv
#   激活: source .venv/bin/activate
#   安装依赖: pip install -r requirements.txt
#
# 或直接使用虚拟环境路径:
#   .venv/bin/python superpicky_cli.py

# 核心依赖
ultralytics>=8.0.0
opencv-python>=4.0.0
# rawpy>=0.18.0  # 注意：rawpy仅支持Python <=3.11，Python 3.12+需要从源码编译或使用替代方案
imageio>=2.0.0
numpy>=1.20.0
Pillow>=9.0.0
pillow-heif>=0.13.0

# AI 模型依赖
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0

# UI 框架 (PySide6 分支)
PySide6>=6.6.0

# 说明：
# - PySide6: Qt6 GUI 框架，替代 Tkinter
# - Pillow: 必需，用于图片处理

